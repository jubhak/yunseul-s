<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <Nullable>disable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <AssemblyName>YUNA-S-new</AssemblyName>
    <RootNamespace>YunaS</RootNamespace>
    <ApplicationManifest>app.manifest</ApplicationManifest>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="LibreHardwareMonitorLib" Version="0.9.4" />
    <PackageReference Include="System.Management" Version="10.0.3" />
    <PackageReference Include="System.Diagnostics.PerformanceCounter" Version="9.0.0" />
  </ItemGroup>

  <!-- 로컬 DLL 참조 (DiskInfoToolkit) -->
  <ItemGroup>
    <Reference Include="DiskInfoToolkit">
      <HintPath>DiskInfoToolkit.dll</HintPath>
    </Reference>
  </ItemGroup>

  <!-- 로컬 DLL을 출력 디렉토리에 복사 + publish에 포함 -->
  <ItemGroup>
    <Content Include="DiskInfoToolkit.dll" CopyToOutputDirectory="PreserveNewest" CopyToPublishDirectory="PreserveNewest" ExcludeFromSingleFile="true" />
  </ItemGroup>

</Project>
